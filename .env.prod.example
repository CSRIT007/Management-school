# Copy to .env on your EC2 server before running ./scripts/deploy.sh
#   cp .env.prod.example .env
#   nano .env

POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me_to_a_strong_password
POSTGRES_DB=management_school

# Docker internal hostname "db" — keep this for EC2 simple (docker-compose.prod.yml)
DATABASE_URL=postgresql://postgres:change_me_to_a_strong_password@db:5432/management_school

# Your EC2 public IP or domain (no trailing slash)
PUBLIC_URL=http://YOUR_EC2_PUBLIC_IP

# Web port exposed on the server (80 for standard HTTP demo)
WEB_PORT=80
